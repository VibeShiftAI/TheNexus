# Task: Fix 404 error on Calendar page

Investigate and fix the 404 error on the Calendar page when viewed through nexus.vibeshiftai.com. Check the Next.js routing, ensure the Calendar route exists and is configured correctly, and verify if there's any discrepancy between local and deployed environments causing the 404.

---
## Completion Instructions
When done, send a POST request to notify the orchestrator:

```bash
curl -X POST http://127.0.0.1:54322/callback \
  -H "Content-Type: application/json" \
  -d '{"taskId": "gc-1776718776761", "nexusProjectId": "c0117b65-9ad7-4afa-90e6-c675b483ccc3", "nexusTaskId": "601385cd-6cc5-45e4-84fa-362ba59bcb63", "conversationId": "<extract your conversation ID from the path of your brain directory>", "result": "<summary of what you did>"}'
```

## If You Cannot Complete This Task
If you encounter an error you cannot resolve, you MUST still send the callback.
Include `"failed": true` and describe the error in the result:

```bash
curl -X POST http://127.0.0.1:54322/callback \
  -H "Content-Type: application/json" \
  -d '{"taskId": "gc-1776718776761", "nexusProjectId": "c0117b65-9ad7-4afa-90e6-c675b483ccc3", "nexusTaskId": "601385cd-6cc5-45e4-84fa-362ba59bcb63", "conversationId": "<extract your conversation ID from the path of your brain directory>", "result": "<what went wrong>", "failed": true}'
```
**The callback is critical** — without it, Praxis cannot track this task's outcome.
