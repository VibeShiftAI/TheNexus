# Task: Fix The Nexus API Key Error

Check The Nexus backend configuration. The user reported: 'Could not connect to AI service, make sure your API key is configured'. Inspect the .env files or configuration files, locate the missing API key, and restore it. If you need to generate one or copy it from another project (like GravityClaw), do so securely. Restart The Nexus server if necessary.

---
## Completion Instructions
When done, send a POST request to notify the orchestrator:

```bash
curl -X POST http://127.0.0.1:54322/callback \
  -H "Content-Type: application/json" \
  -d '{"taskId": "gc-1774697339607", "nexusProjectId": "", "nexusTaskId": "", "result": "<summary of what you did>"}'
```
