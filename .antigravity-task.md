# Task: Update daily token limit in .env to 4 million

The previous hardcoded update to 4,000,000 didn't take effect, likely because it is overridden by an environment variable in a .env file. Find the relevant .env file in the workspace, update the daily limit to 4000000, and restart the Nexus API service.

---
## Completion Instructions
When done, send a POST request to notify the orchestrator:

```bash
curl -X POST http://127.0.0.1:54322/callback \
  -H "Content-Type: application/json" \
  -d '{"taskId": "gc-1776457335628", "nexusProjectId": "", "nexusTaskId": "", "conversationId": "<extract your conversation ID from the path of your brain directory>", "result": "<summary of what you did>"}'
```

## If You Cannot Complete This Task
If you encounter an error you cannot resolve, you MUST still send the callback.
Include `"failed": true` and describe the error in the result:

```bash
curl -X POST http://127.0.0.1:54322/callback \
  -H "Content-Type: application/json" \
  -d '{"taskId": "gc-1776457335628", "nexusProjectId": "", "nexusTaskId": "", "conversationId": "<extract your conversation ID from the path of your brain directory>", "result": "<what went wrong>", "failed": true}'
```
**The callback is critical** — without it, Praxis cannot track this task's outcome.
