# Task: Nexus Task 60879a3c-3506-4fc7-9e90-4e2232ef41cd

Execute Nexus task 60879a3c-3506-4fc7-9e90-4e2232ef41cd. Check the task details in TheNexus dashboard for full context.

---
## Completion Instructions
When done, send a POST request to notify the orchestrator:

```bash
curl -X POST http://127.0.0.1:54322/callback \
  -H "Content-Type: application/json" \
  -d '{"taskId": "gc-1776514260472", "nexusProjectId": "", "nexusTaskId": "60879a3c-3506-4fc7-9e90-4e2232ef41cd", "conversationId": "<extract your conversation ID from the path of your brain directory>", "result": "<summary of what you did>"}'
```

## If You Cannot Complete This Task
If you encounter an error you cannot resolve, you MUST still send the callback.
Include `"failed": true` and describe the error in the result:

```bash
curl -X POST http://127.0.0.1:54322/callback \
  -H "Content-Type: application/json" \
  -d '{"taskId": "gc-1776514260472", "nexusProjectId": "", "nexusTaskId": "60879a3c-3506-4fc7-9e90-4e2232ef41cd", "conversationId": "<extract your conversation ID from the path of your brain directory>", "result": "<what went wrong>", "failed": true}'
```
**The callback is critical** — without it, Praxis cannot track this task's outcome.
