# Environment
.env
.env.local
db/.env.supabase

# Media adapter secrets (YouTube OAuth refresh token, etc.) and artifacts
.secrets/
nexus-builder/data/media/
nexus-builder/data/media_cost_ledger.db*

# Cloudflare Tunnel credentials
cert.pem
cloudflared_config.yml

# Debug logs
server-debug.log

# Dependencies
node_modules/

# Python virtual environment
venv/
.venv/
ENV/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/

# Cloudflare
.cloudflared/

# Checkpoint archives (can contain sensitive conversation data)
checkpoint_archives/*.json

# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
logs/

# OS files
.DS_Store
Thumbs.db

# Build outputs
dist/
build/
.next/
# …but the desktop shell's static pages are source, not build output
!desktop/dist/
!desktop/dist/**

# Staged travel-app update payloads (signed installers + manifest) — served
# through the tunnel by /api/updates, produced by CI, never committed.
desktop/updates/

# Rust/Cargo build output (Tauri) — build locally, never commit
target/

# Neo4j data files
docker/data/neo4j_data/

# SQLite databases and temp files
*.db
*.db-shm
*.db-wal
*.db.*
*.sqlite
*.sqlite3

# DB backups / snapshots (timestamped names slip past *.db)
backups/

# Blackboard session data
data/blackboard/

# Local-only files
.worktrees/
start-nexus.bat
Start The Nexus.bat
Start The Nexus.command
stop-nexus.bat
start-local.bat
start-local.sh
stop-nexus.sh
pinned.json
TheNexus.code-workspace
nodemon.json

# Build artifacts — too large for GitHub (100MB limit); build locally instead
dashboard/public/*.apk
