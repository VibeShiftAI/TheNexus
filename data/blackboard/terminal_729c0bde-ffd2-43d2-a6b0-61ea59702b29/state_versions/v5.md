<!-- version:5 -->
001: ## User_Query
002: 
003: 
004: ## Plan
005: 
006: 
007: ## Notes
008: 
009: 
010: 
011: ## [2026-02-22T18:55:53.887247] architect
012: # Plan: ChoreTracker - Weekly Household Task Manager (v1)
013: 
014: # ChoreTracker Project Plan
015: 
016: This project aims to build a local-first web application to track household chores. The system calculates upcoming chores for the week based on frequency and history (who performed the task last).
017: 
018: ## Tech Stack
019: - **Framework**: Next.js (App Router)
020: - **Styling**: Tailwind CSS + Shadcn UI
021: - **Database**: SQLite with Drizzle ORM (Local file-based storage)
022: - **Icons**: Lucide-react
023: 
024: ---
025: 
026: ### Task 1: Project Initialization & UI Foundation
027: - **Workflow**: Nexus Prime
028: - **Goal**: Initialize the Next.js project with Tailwind CSS and install necessary UI components.
029: - **Context & Execution**: Create a new Next.js project. Install Shadcn UI and set up a basic layout with a sidebar for navigation (Dashboard, Chores, Household). Use `lucide-react` for icons.
030: - **Acceptance Criteria**:
031:   - Next.js app is running on localhost:3000.
032:   - Basic layout with navigation is visible.
033:   - Tailwind and Shadcn are configured correctly.
034: 
035: ### Task 2: Database Schema & Drizzle Setup
036: - **Workflow**: Nexus Prime
037: - **Goal**: Define the data model for users, chores, and chore history using SQLite.
038: - **Context & Execution**: Setup Drizzle ORM with `better-sqlite3`. Define the following tables:
039:   - `users`: id, name, avatar_url
040:   - `chores`: id, name, description, frequency_days (integer)
041:   - `chore_completions`: id, chore_id, user_id, completed_at
042: - **Acceptance Criteria**:
043:   - `schema.ts` file exists with defined relations.
044:   - Database migrations can be generated and applied.
045:   - A local `.db` file is initialized.
046: 
047: ### Task 3: Household Management (Users)
048: - **Workflow**: Nexus Prime
049: - **Goal**: Create the interface to add and manage household members.
050: - **Context & Execution**: Build a "Household" page with a list of users and a form/dialog to add a new user. Store users in the `users` table.
051: - **Acceptance Criteria**:
052:   - User can add a household member by name.
053:   - List of members displays accurately from the database.
054: 
055: ### Task 4: Chore Definition Management
056: - **Workflow**: Nexus Prime
057: - **Goal**: Create the interface to define chores and their frequencies.
058: - **Context & Execution**: Build a "Chores" page. Each chore needs a name and a frequency in days (e.g., 'Vacuuming' every 7 days). Implement CRUD operations using Server Actions.
059: - **Acceptance Criteria**:
060:   - User can create, edit, and delete chores.
061:   - Frequency is stored as an integer representing days.
062: 
063: ### Task 5: Chore Engine & Due Date Logic
064: - **Workflow**: Nexus Prime
065: - **Goal**: Implement the logic to determine when a chore is due and who should do it.
066: - **Context & Execution**: Create a utility function `getWeeklySummary()` that:
067:   1. Looks at the last completion date for each chore.
068:   2. Calculates the next due date based on frequency.
069:   3. If due within the next 7 days, includes it in the summary.
070:   4. Suggests the next assignee based on who performed it least recently among all users.
071: - **Acceptance Criteria**:
072:   - Function correctly identifies chores due in the current week.
073:   - Assignment logic correctly picks the user with the oldest completion timestamp for that specific chore.
074: 
075: ### Task 6: Weekly Dashboard UI
076: - **Workflow**: Nexus Prime
077: - **Goal**: Build the main dashboard showing the "Weekly Summary".
078: - **Context & Execution**: Create a dashboard view that displays cards for each chore due this week. Each card should show: Chore Name, Due Date, and "Suggested Assignee". Include a "Mark as Done" button.
079: - **Acceptance Criteria**:
080:   - Dashboard renders a list of tasks for the upcoming week.
081:   - "Mark as Done" button triggers a server action that logs a new completion.
082: 
083: ### Task 7: Completion Logging & History
084: - **Workflow**: Nexus Prime
085: - **Goal**: Finalize the feedback loop by recording task completions.
086: - **Context & Execution**: When a user marks a chore as done, prompt them to select which user actually did it (default to the suggested user). Save this to `chore_completions`. Refresh the dashboard state immediately.
087: - **Acceptance Criteria**:
088:   - Clicking "Mark as Done" adds a record to the database.
089:   - The dashboard updates, moving the chore to the bottom or removing it from the "Due" list for the remainder of its cycle.
090: 
091: ## [2026-02-22T18:57:02.558510] council
092: ## Council Review
093: 
094: ### Frontend/UX Specialist: APPROVE
095: This is a well-scoped plan for a local-first prototype. Tasks provide clear goals and acceptance criteria that are actionable for a rapid implementation using Next.js, Drizzle + SQLite, Tailwind, and Shadcn UI. The plan focuses on the right order (data layer → logic → UI) and uses Server Actions where appropriate. I recommend a few pragmatic additions around accessibility, responsive behavior, schema details (timestamps, foreign keys, indices), edge cases in the chore-assignment algorithm, and UX for loading/error/empty states to make the implementation robust and the user flow intuitive. None of these are blockers for local prototyping; they are suggestions to improve quality and reduce rework.
096: 
097: **Line-Level Feedback:**
098: - **Line 8**: Good choice. The App Router and Server Actions fit the server-first data flow described, but the plan should call out where to use server components vs client components (e.g., forms, dialogs, and Mark as Done flows).
099:   - *Suggestion*: Add a short note: 'Use Server Components for fetching DB-driven pages and Server Actions for CRUD; make interactive pieces (dialogs, mark-done button, forms) client components to handle local state and optimistic updates.'
100: - **Line 9**: Shadcn UI provides accessible primitives, but accessibility and design tokens should be explicitly considered (contrast, focus styles, dark mode).
101:   - *Suggestion*: Specify baseline accessibility and responsiveness requirements (WCAG AA contrast, visible focus rings, mobile-first breakpoints). Mention configuring Tailwind theme tokens for spacing/typography.
102: - **Line 10**: SQLite + Drizzle is appropriate for local-first prototyping. The plan should clarify migration tooling, location of the .db file, and simple seed data for dev/test.
103:   - *Suggestion*: Add a line: 'Use drizzle-kit or similar for migrations. Put .db in a repo-ignored /data or /db path and include a seed script for initial users/chores.'
104: - **Line 11**: Lucide is fine; ensure icons are accessible (aria-hidden on decorative icons and aria-label/title for actionable icons).
105:   - *Suggestion*: Document that icon buttons must include accessible labels and that decorative icons use aria-hidden.
106: - **Line 16**: This workflow label appears on every task; its meaning isn't clear in this context.
107:   - *Suggestion*: Either define what 'Nexus Prime' means for the team (e.g., a cadence or ticket tag) or remove it if it's not relevant to implementation details.
108: - **Line 18**: Sidebar nav is fine, but responsive/keyboard behavior and skip links should be considered up front.
109:   - *Suggestion*: Add acceptance details: 'Sidebar collapses to a hamburger on small screens, includes a visible skip link to main content, and supports keyboard navigation and ARIA landmarks.'
110: - **Line 27**: Table shapes are listed, but missing common fields and constraints that simplify queries and sorting (timestamps, foreign keys, indices).
111:   - *Suggestion*: Add created_at/updated_at timestamps, explicit foreign keys on chore_completions, and indices on chore_id/user_id/completed_at. Consider a boolean 'active' on chores and an optional 'timezone' strategy note (store UTC timestamps).
112: - **Line 28**: Minimal user model is okay for prototype, but consider fields that help UX and sorting.
113:   - *Suggestion*: Add optional 'display_name'/'initials' logic, 'created_at' timestamp, and ensure avatar_url can be null (with placeholder).
114: - **Line 29**: Frequency as days is simple and works, but add validation and optional fields for exceptions or disabled state.
115:   - *Suggestion*: Enforce frequency_days >= 1, add 'active' boolean, optional 'last_completed_at' cache only if needed (or compute from completions), and consider a human-readable cadence field for UI help text.
116: - **Line 56**: Edge case: chores with no history need a defined behavior (who should do the first run?). Also consider timezones and inclusive/exclusive calculation for 'next due'.
117:   - *Suggestion*: Specify fallback: 'If no completions, treat chore as due and prioritize users who have never done it; document timezone handling (store UTC, compute local dates for display).'
118: - **Line 59**: The rule is clear but needs tie-breakers (users who've never done it) and a deterministic fallback. Also clarify whether assignment is per-chore (track last-performed per chore) or global fairness.
119:   - *Suggestion*: Enhance acceptance criteria: 'If a user has never completed the chore, prioritize them. If multiple candidates tie, pick deterministically (e.g., lowest id) or round-robin per chore). Add unit tests for these cases.'
120: - **Line 67**: UI is clear; include behavior for loading, empty states, error handling, and keyboard/assistive interactions for the card and the action button.
121:   - *Suggestion*: Add acceptance details: 'Cards have accessible headings, announce updates when items are marked done, disable the button while saving, show optimistic UI or loading indicator, and include an empty-state illustration and CTA to add chores.'
122: - **Line 75**: Good flow. Clarify whether the prompt is a modal or inline, and ensure focus management plus keyboard support. Also define immediate refresh strategy: optimistic update or re-fetch after server action.
123:   - *Suggestion*: Recommend a small modal with focus trap and keyboard shortcuts, default selection pre-filled, and use optimistic UI to update the dashboard while the server action completes; fall back with an error toast on failure.
124: 
125: ---
126: ### Systems Engineer: REQUEST_INFO
127: Review failed: 1 validation error for VoteReceipt
128: line_comments
129:   Input should be a valid list [type=list_type, input_value='[\n  {\n    "voter": "Se...data mismatch."\n  }\n]', input_type=str]
130:     For further information visit https://errors.pydantic.dev/2.12/v/list_type
131: 
132: ---
133: ### Senior QA Strategist: REQUEST_INFO
134: The plan is well-structured for a rapid prototype, but the core 'Chore Engine' (Task 5) lacks specific acceptance criteria for edge cases that are likely to break the logic. Specifically, the assignment logic does not define behavior for chores with zero completion history or households with no users. Since the execution engine relies on concrete success criteria for validation, these logic paths must be explicitly defined. Additionally, basic data validation (e.g., preventing zero or negative frequency days) is missing from the CRUD acceptance criteria.
135: 
136: **Line-Level Feedback:**
137: - **Line 37**: The schema definition lacks constraints for the frequency_days integer.
138:   - *Suggestion*: Specify that frequency_days must be a positive integer (greater than 0) in the acceptance criteria.
139: - **Line 61**: This logic has a critical edge case: if a chore is new and has no completion history, the 'least recent' calculation might return null or fail.
140:   - *Suggestion*: Add an acceptance criterion defining that for chores with no history, the engine should default to the user with the overall lowest total chore count or pick alphabetically from the household list.
141: - **Line 66**: This criterion is not verifiable without a fixed reference date.
142:   - *Suggestion*: Update to: 'Function correctly identifies chores due within a 7-day window when provided a specific mock current date.'
143: - **Line 75**: Missing acceptance criteria for the 'Empty State' when no chores are due.
144:   - *Suggestion*: Add: 'If no chores are due, the dashboard displays a specific "all clear" message or empty state illustration.'
145: - **Line 84**: The 'remainder of its cycle' is ambiguous for validation.
146:   - *Suggestion*: Update to: 'The dashboard immediately removes the chore from the "Due" list and recalculates its next due date based on the new completion timestamp.'
147: 
148: ---
149: ### Senior Gap Analyst: REQUEST_INFO
150: The plan covers core CRUD, logic, and UI for a simple local-first chore tracker effectively. No critical gaps block execution or launch. However, moderate gaps exist in feature completeness (user management, history UI, initial chore due dates), robustness (error handling, validation), and implied integrations (data fetching in dashboard assumed but not explicit). These degrade quality but are fixable. Proportional to simple prototyping scope; no enterprise gaps flagged.
151: 
152: **Line-Level Feedback:**
153: - **Line 41**: 'Manage' implies edit and delete capabilities, but acceptance criteria only specify adding and listing. Missing: edit (name/avatar) and delete users.
154:   - *Suggestion*: Add AC bullets: '- Users can be edited (name, optional avatar). - Users can be safely deleted (handle or cascade completions).' Moderate severity: degrades household management UX.
155: - **Line 62**: Logic assumes chores have completion history. Missing: handling for new chores (no completions) - next due date undefined.
156:   - *Suggestion*: Specify: 'If no completions, treat last_completed_at as null and set next_due to current date.' Update function and tests. Moderate: new chores won't appear otherwise.
157: - **Line 3**: Emphasizes 'history', schema includes chore_completions, logging implemented, but no UI to view history.
158:   - *Suggestion*: Add Task 8: History page or section (e.g., list past completions per chore, filter by user). Moderate: feature implied but overlooked.
159: - **Line 77**: No error recovery or loading states planned for server actions (e.g., DB errors, validation fails). No user feedback.
160:   - *Suggestion*: In server actions: use try-catch, Zod validation, return {success, error}; UI: loading spinner, error toast (e.g., Sonner). Moderate: poor UX on failures.
161: - **Line 54**: No validation for inputs (e.g., chore name required, frequency positive integer). Risk of invalid data.
162:   - *Suggestion*: Add client/server validation (Zod): required name, frequency >=1. AC: '- Invalid inputs show errors.' Minor-moderate.
163: 
164: 
165: ## [2026-02-22T18:57:51.583977] architect
166: # Plan: Nexus Prototype: Local-First Chore Tracking OS (v2)
167: 
168: # Project Overview
169: This project builds a local-first web application to track household chores. It calculates upcoming chores for the week based on frequency and history (who performed the task last).
170: 
171: ## Architecture & Conventions
172: - **Framework**: Next.js (App Router). Use Server Components for fetching DB-driven pages and Server Actions for CRUD. Make interactive pieces (dialogs, mark-done button, forms) client components to handle local state and optimistic updates.
173: - **Styling**: Tailwind CSS + Shadcn UI. Enforce baseline accessibility and responsiveness (WCAG AA contrast, visible focus rings, mobile-first breakpoints). Configure Tailwind theme tokens for spacing/typography.
174: - **Database**: SQLite with Drizzle ORM (`better-sqlite3`). Use `drizzle-kit` for migrations. Store the local `.db` file in a repo-ignored `/data` or `/db` path and include a seed script for initial users/chores. Store all timestamps in UTC.
175: - **Icons**: `lucide-react`. Icon buttons must include accessible labels, and decorative icons must use `aria-hidden="true"`.
176: 
177: ---
178: 
179: ### Task 1: Project Initialization & Layout Setup
180: - **Workflow**: Nexus Prime
181: - **Goal**: Scaffold the Next.js application, install required UI libraries, and implement the main application shell and navigation.
182: - **Context & Execution**: Initialize a new Next.js app. Install Tailwind CSS, Shadcn UI, and `lucide-react`. Create a root layout with a sidebar navigation containing links to Dashboard, Chores, Household, and History. Implement a mobile-responsive wrapper.
183: - **Acceptance Criteria**:
184:   - The Next.js application runs locally without errors.
185:   - The sidebar collapses to a hamburger menu on small screens.
186:   - The layout includes a visible skip link to the main content.
187:   - Supports full keyboard navigation and utilizes proper ARIA landmarks (nav, main).
188: 
189: ### Task 2: Database Schema & Migration Setup
190: - **Workflow**: Nexus Prime
191: - **Goal**: Define the Drizzle ORM schema for users, chores, and completions, and set up the local SQLite database.
192: - **Context & Execution**: Install `drizzle-orm`, `drizzle-kit`, and `better-sqlite3`. Define the schema in `db/schema.ts` with the following tables: `users` (id, name, display_name optionally for initials, avatar_url nullable, created_at); `chores` (id, name, description, frequency_days as integer, active as boolean default true, cadence_text optional, created_at, updated_at); `chore_completions` (id, chore_id, user_id, completed_at). Ensure explicit foreign keys on `chore_completions` (cascading deletes where appropriate) and add indices on `chore_id`, `user_id`, and `completed_at`. Create a seed script in a `scripts/` folder.
193: - **Acceptance Criteria**:
194:   - Schema correctly applies `created_at` and `updated_at` timestamps.
195:   - `drizzle-kit` generates a valid migration and the seed script successfully populates initial mock data.
196:   - `frequency_days` is constrained to positive integers.
197: 
198: ### Task 3: Household Member Management
199: - **Workflow**: Nexus Prime
200: - **Goal**: Create the interface to add, edit, and safely manage household members (users).
201: - **Context & Execution**: Create a page at `/household`. Use Server Actions with Zod validation to handle user creation, updates, and deletions. Ensure deletions safely cascade or handle existing chore completions to avoid orphaned records. Incorporate a loading spinner during saves and error toasts via Sonner for failures.
202: - **Acceptance Criteria**:
203:   - Users can be created, edited (name, avatar), and safely deleted.
204:   - Invalid inputs show clear validation errors.
205:   - Operations return `{success, error}` status and handle UI feedback properly.
206: 
207: ### Task 4: Chore Management (CRUD)
208: - **Workflow**: Nexus Prime
209: - **Goal**: Build the interface to add, edit, and toggle active status of household chores.
210: - **Context & Execution**: Create a page at `/chores`. Implement form using Shadcn UI components. Use Zod validation to enforce `frequency_days >= 1` and require a name. Use Server Actions to save data to the `chores` table.
211: - **Acceptance Criteria**:
212:   - Chores can be created, edited, and toggled active/inactive.
213:   - Client and Server validation strictly blocks creation if `frequency_days < 1`.
214:   - Invalid inputs show inline errors and error toasts.
215: 
216: ### Task 5: Core Chore Engine Logic
217: - **Workflow**: Nexus Prime
218: - **Goal**: Implement the algorithm to calculate upcoming chores and assign them to users based on history.
219: - **Context & Execution**: Create a service utility (e.g., `lib/chore-engine.ts`) to calculate due chores. Logic requirements: If a chore has no completions, treat `last_completed_at` as null and set `next_due` to the current date. Assignee suggestion looks at the least recent user. If no history exists for a chore, default to the user with the lowest overall total chore count, breaking ties deterministically by lowest user ID or alphabetically. Store dates in UTC but evaluate against a 7-day window.
220: - **Acceptance Criteria**:
221:   - Function correctly identifies chores due within a 7-day window when provided a specific mock current date.
222:   - Deterministically suggests users for chores with no completion history.
223:   - Includes unit tests validating edge cases (no completions, empty households, tie-breakers).
224: 
225: ### Task 6: Dashboard UI Implementation
226: - **Workflow**: Nexus Prime
227: - **Goal**: Display a dashboard of chores due this week with a clear interface.
228: - **Context & Execution**: Create the `/` (Dashboard) page. Fetch data using Server Components integrating the Chore Engine logic. Display cards for each due chore indicating Chore Name, Due Date, and Suggested Assignee. Provide a "Mark as Done" button on each card.
229: - **Acceptance Criteria**:
230:   - Cards possess accessible headings and announce updates to screen readers.
231:   - If no chores are due, the dashboard displays a specific "all clear" message or empty-state illustration with a CTA to add chores.
232:   - Layout is fully responsive, looking good on mobile.
233: 
234: ### Task 7: "Mark as Done" Flow & Optimistic Updates
235: - **Workflow**: Nexus Prime
236: - **Goal**: Build the completion interaction, allowing users to confirm or override the suggested assignee.
237: - **Context & Execution**: Enhance the dashboard "Mark as Done" button. Clicking opens a small modal (with focus trap and keyboard shortcuts) to select which user completed the chore. Pre-fill the suggested user. Upon submission, trigger a Server Action with try-catch and Zod validation. Implement optimistic UI to immediately update the dashboard.
238: - **Acceptance Criteria**:
239:   - Modal traps focus and supports ESC to close.
240:   - Submitting immediately removes the chore from the "Due" list and recalculates its next due date based on the new timestamp (optimistic UI).
241:   - Button disables while saving; fall back with an error toast (Sonner) on failure.
242:   - Validates inputs before saving to `chore_completions`.
243: 
244: ### Task 8: History View
245: - **Workflow**: Nexus Prime
246: - **Goal**: Create a page to view past chore completions.
247: - **Context & Execution**: Create a page at `/history` fetching from `chore_completions`, joined with `users` and `chores`. Provide basic filters (by user or by chore) and sort by `completed_at` descending.
248: - **Acceptance Criteria**:
249:   - Page renders a list or table of historical completions.
250:   - Users can filter the list to see what a specific person did or the history of a specific chore.
251:   - Uses Server Components for data fetching.
252: 
253: ## [2026-02-22T18:58:54.340567] council
254: ## Council Review
255: 
256: ### Senior Frontend/UX Specialist: APPROVE
257: The plan is exceptionally well-structured for a rapid prototype, demonstrating a strong understanding of both technical implementation (Next.js App Router, Drizzle, optimistic updates) and user-centric design. The inclusion of accessibility features like skip links, keyboard navigation, and ARIA landmarks from the initial task is commendable. The user flows are logical, particularly the completion flow with its focus on interactive refinements like modals and optimistic UI updates for a 'local-first' feel.
258: 
259: **Line-Level Feedback:**
260: - **Line 19**: Proactive inclusion of skip links is an excellent accessibility practice for keyboard and screen reader users.
261: - **Line 70**: Focus trapping and keyboard handling are critical for modal UX. Shadcn's Dialog component is well-suited for this as it handles the underlying Radix primitives.
262:   - *Suggestion*: Ensure the modal returns focus to the trigger button upon closing to prevent keyboard focus loss and maintain the user's position in the chore list.
263: - **Line 80**: Using URL search parameters for filtering in Next.js allows for a superior user experience, enabling bookmarking, shareable links, and native browser history support for filtered views.
264:   - *Suggestion*: Utilize Next.js searchParams to drive the filtering logic server-side for better performance and state management.
265: 
266: ---
267: ### Systems Engineer: APPROVE
268: This is a well-structured, appropriately scoped plan for a local-first household chore tracker. The architecture choices are coherent and well-matched to the problem: Next.js App Router with Server Components and Server Actions is a solid fit, SQLite + Drizzle ORM is perfectly appropriate for a local-first app, and the UI stack (Tailwind + Shadcn + Sonner) is pragmatic. The author has clearly addressed prior council feedback — schema indices, cascading deletes, UTC timestamps, accessibility requirements, optimistic updates, and edge-case handling in the chore engine are all present. A few targeted issues remain worth flagging, but none are blockers for a prototype of this scope.
269: 
270: **Line-Level Feedback:**
271: - **Line 22**: Good call on gitignoring the .db file. Make sure the seed script is idempotent — i.e., it checks for existing records before inserting — so re-running it during development doesn't create duplicate users or chores.
272:   - *Suggestion*: Add a guard in the seed script: e.g., `if (db.select().from(users).all().length === 0) { /* seed */ }`. This prevents duplicate data on repeated runs.
273: - **Line 23**: Correct and important. SQLite has no native timestamp type — Drizzle will store these as ISO 8601 text or Unix integers. Make sure the schema explicitly uses `integer('created_at', { mode: 'timestamp' })` or `text` with a consistent format, and that all application code uses `new Date().toISOString()` or `Date.now()` rather than locale-aware constructors.
274:   - *Suggestion*: Document the chosen timestamp storage mode (integer epoch vs. ISO text) in a comment at the top of `db/schema.ts` so all contributors use it consistently.
275: - **Line 35**: SQLite does NOT enforce foreign keys by default. You must run `PRAGMA foreign_keys = ON;` on every connection. With better-sqlite3, this means calling `db.pragma('foreign_keys = ON')` immediately after opening the connection. Without this, cascading deletes will silently do nothing.
276:   - *Suggestion*: In your Drizzle client setup file (e.g., `db/index.ts`), add: `db.pragma('foreign_keys = ON')` right after `const db = new Database(...)`. Add a comment explaining why.
277: - **Line 36**: Good. For the history view and chore engine queries, a composite index on `(chore_id, completed_at DESC)` will be more efficient than separate single-column indices for the most common query pattern (latest completion per chore).
278:   - *Suggestion*: Consider replacing or supplementing the individual `chore_id` index with a composite: `index('chore_completions_chore_date_idx').on(chore_completions.chore_id, chore_completions.completed_at)`.
279: - **Line 48**: The cascade strategy needs a deliberate decision: cascading deletes on `chore_completions` when a user is deleted will permanently erase history, which may be undesirable. Consider a soft-delete approach (e.g., `active` boolean on `users`) or setting `user_id` to NULL on completions (requires nullable FK) to preserve history.
280:   - *Suggestion*: For a household app, soft-deleting users (setting `active = false`) is likely the better UX. This preserves historical completion records and avoids data loss. If hard delete is preferred, at minimum show a confirmation warning in the UI that history will be erased.
281: - **Line 63**: Zod validation in Task 4 handles the application-layer constraint, which is correct. SQLite has no CHECK constraint support via Drizzle's schema DSL in all versions — verify your Drizzle version supports `.check()` or add the constraint manually in the migration SQL to enforce this at the DB layer as well.
282:   - *Suggestion*: In the migration SQL, add: `CHECK (frequency_days >= 1)` on the `chores` table. This provides a database-level safety net independent of application validation.
283: - **Line 80**: The tie-breaking rule mentions 'lowest user ID or alphabetically' — pick one and document it. Mixing strategies (numeric ID vs. alphabetical name) can produce inconsistent results if user IDs are UUIDs or non-sequential. Lowest user ID (by insertion order or integer PK) is the more stable and deterministic choice.
284:   - *Suggestion*: Standardize on lowest integer user ID as the tie-breaker in `lib/chore-engine.ts` and add a comment: `// Tie-break by lowest user ID (insertion order) for determinism.`
285: - **Line 81**: The 7-day window evaluation needs to be timezone-aware on the client side. If a user's local timezone is UTC-8, 'today' for them may differ from UTC midnight. For a local-first app, consider using the server's local time (which is the user's machine) rather than strict UTC for window evaluation, or document that the app assumes the server timezone matches the user's timezone.
286:   - *Suggestion*: In `chore-engine.ts`, derive the window boundaries using local date arithmetic (e.g., start of today in local time) rather than UTC midnight, since this is a single-user local app. Add a comment explaining the timezone assumption.
287: - **Line 96**: Shadcn UI's Dialog component includes a built-in focus trap via Radix UI primitives, so this is covered without custom implementation. Just ensure the modal's initial focus lands on the user selector (or a logical first interactive element), not the close button, for the best screen reader experience.
288:   - *Suggestion*: Use Shadcn's `<Dialog>` with `<DialogContent>`. Set `autoFocus` on the user `<Select>` or first form field inside the modal to ensure focus lands in the right place on open.
289: - **Line 98**: With Next.js Server Actions, optimistic updates can be implemented via `useOptimistic` (React 19 / Next.js 14+). Ensure the rollback path on failure is explicit — the error toast alone is not sufficient if the optimistic state isn't reverted. The chore card should reappear in the list if the Server Action throws.
290:   - *Suggestion*: Use `useOptimistic` with a reducer that removes the chore on submission and restores it on error. Pair with `startTransition` to keep the UI responsive. Test the failure path explicitly.
291: - **Line 113**: For a local SQLite app with modest data volumes, client-side filtering is perfectly acceptable and simpler to implement. If you opt for server-side filtering via URL search params (which is more Next.js-idiomatic with Server Components), ensure the filter params are validated/sanitized before being passed to the query to prevent unexpected behavior.
292:   - *Suggestion*: If using URL search params for filters, validate them with Zod before use: `const parsed = z.object({ userId: z.string().optional(), choreId: z.string().optional() }).parse(searchParams)`. This prevents type errors from malformed URLs.
293: 
294: ---
295: ### Senior QA Strategist: APPROVE
296: The plan provides clear and verifiable acceptance criteria for each task, suitable for a rapid-prototyping environment. Core logic in Task 5 explicitly requires unit tests for edge cases, enhancing verifiability. UI tasks address accessibility, responsiveness, and error states (toasts, spinners). Database tasks specify schema constraints, migrations, and seeds testable via inspection or queries. Integration points (Server Actions, optimistic updates) include validation, error handling, and UI feedback. No separate QA phases needed as criteria align with automated engine validation. Minor suggestions for explicit error rollback and DB verification enhance robustness without warranting rejection.
297: 
298: **Line-Level Feedback:**
299: - **Line 45**: Clear and basic verifiability; engine can confirm no runtime errors on startup.
300: - **Line 72**: Good schema-level constraint; verifiable via Drizzle schema inspection or failed insert attempts.
301:   - *Suggestion*: Add AC for verifying constraint enforcement via a test insert with invalid data.
302: - **Line 92**: Excellent; explicit unit testing strategy for critical algorithm covers edge cases, boundary conditions, and determinism.
303: - **Line 118**: Verifiable optimistic update behavior; good coverage of integration between UI, engine, and DB.
304:   - *Suggestion*: Explicitly state optimistic rollback on Server Action failure to ensure consistency.
305: - **Line 133**: Appropriate for server-side rendering; integration with DB and joins testable via rendered output.
306: 
307: ---
308: ### Senior Gap Analyst: REQUEST_INFO
309: The plan is detailed and covers most functional concerns, but several critical integration and operational gaps remain that would block a reliable local-first launch. Missing items include how users/authentication/current-user context is established (essential for marking chores done and for auditing), concrete SQLite/Next.js runtime and connection lifecycle decisions (better-sqlite3 and Server Actions require Node runtime and a robust DB connection pattern), and concurrency/transaction handling for completions to avoid race conditions. Other moderate gaps (timezone handling, deletion policy for users, avatar storage, migration rollback, pagination for history) should be resolved before implementation. I request clarification and decisions on the highlighted items below so the plan can be executed safely and predictably.
310: 
311: **Line-Level Feedback:**
312: - **Line 4**: [critical] Missing a runtime & DB-driver compatibility decision and a DB connection lifecycle strategy. better-sqlite3 is a native Node package and will not work on Edge runtimes — Next.js App Router pages/components can run in different runtimes. Also, opening multiple SQLite connections without a singleton pattern can crash or corrupt the DB during development (hot-reload) or in multi-process setups.
313:   - *Suggestion*: Specify that all DB-accessing code (Server Actions, Server Components that query) will run in the Node runtime (not Edge). Define a singleton DB connection module (with proper open/close semantics) and document expected host environment (local-only Node server). Add guidance on journal_mode (WAL), busy_timeout, and using transactions for multi-statement changes.
314: - **Line 6**: [critical] Missing concurrency/safety and backup/restore strategy for a local-first SQLite DB. The plan doesn't say how the DB file is accessed across processes, how to handle DB corruption, backups, or how to migrate/rollback safely for existing local installs.
315:   - *Suggestion*: Add a section that documents: expected single process vs multi-process usage; recommended SQLite settings (WAL, synchronous); a simple backup/export/import flow (e.g., 'Export DB as .sqlite / JSON' from UI or scripts); and a rollback plan for failing migrations (how to restore from backup). Ensure drizzle-kit migrations are reversible or provide a safe rollback path.
316: - **Line 5**: [minor] Accessibility is called out but automated/audit testing is not included. No acceptance step ensures ARIA semantics and live-region behavior are validated.
317:   - *Suggestion*: Add lightweight automated checks (axe-core in CI/local script or jest-axe unit tests) and an accessibility manual test checklist for keyboard only and screen reader flows, especially for dynamic pieces like the dashboard and modals.
318: - **Line 21**: [moderate] Deletion policy is underspecified. 'Cascade or handle' is ambiguous and has significant data-retention implications (auditing/history).
319:   - *Suggestion*: Decide and document the chosen policy: either (A) disallow deletion if completions exist (require manual reassignment), (B) cascade-delete completions (and warn/confirm), or (C) soft-delete users (mark active=false) while preserving completions. Prefer soft-delete or reassign-to-placeholder for preserving history/audit. Add migration to support chosen approach if needed.
320: - **Line 31**: [critical] Timezone and determinism gaps. "Store dates in UTC but evaluate against a 7-day window" is ambiguous about which timezone to use for 'current date' and for end-user expectations (local vs UTC). Deterministic tie-breaking needs exact rules when sorting by name (case, locale) or ID.
321:   - *Suggestion*: Specify that all calculations run using UTC instants, but present dates converted to the user's local timezone in the UI. Document: (a) how 'current date' is derived (server-now in UTC or client-provided current), (b) whether the 7-day window is inclusive/exclusive, and (c) exact deterministic tie-breaker rules (e.g., compare lower numeric user ID, then lowercase ASCII name). Add tests that cover DST transitions and timezone boundaries.
322: - **Line 36**: [moderate] No definition of current-user selection or presence. The UI needs to know who is using the app (session/current user) to interpret suggested assignee, and to pre-fill 'Mark as Done' forms. The plan treats household members purely as data, but not as runtime identities.
323:   - *Suggestion*: Add either (A) a runtime 'current user' selector persisted in local state (and surfaced in header), or (B) lightweight authentication (pincode or local account). Define expected behavior if no current user is set when a mark-as-done action occurs (force selection modal).
324: - **Line 41**: [critical] Missing reconciliation strategy for optimistic updates and conflict resolution. If another client marks the same chore done almost simultaneously, or the server computes a different next_due, the optimistic update may be inconsistent or cause duplicate completions.
325:   - *Suggestion*: Define optimistic-update reconciliation rules: update UI locally immediately, but reconcile with authoritative server response (apply server next_due and replace optimistic entry). Use DB transactions to insert completions atomically and return canonical next_due. Consider preventing duplicate completions within a small time-window by adding uniqueness constraints (if appropriate) or detect duplicates on Server Action and return a clear status code for the client to handle.
326: - **Line 16**: [moderate] Schema-level constraints & indexing details are incomplete. For example, there is no uniqueness/constraints for repeated completions, and composite indices that support the Chore Engine queries (e.g., querying last completion per chore) aren't suggested.
327:   - *Suggestion*: Add index on (chore_id, completed_at DESC) for efficient 'last completion' lookup. Consider a unique constraint if business logic requires (e.g., prevent multiple completions for same chore at same timestamp). Document how to handle very large completion histories (archival strategy). Also ensure created_at/updated_at have DB defaults (CURRENT_TIMESTAMP) and migrations set them correctly.
328: - **Line 17**: [moderate] Migration rollback and non-destructive migration strategy missing. If a migration fails on a user's local DB, there's no documented recovery.
329:   - *Suggestion*: Add guidance for migration failure handling: require that migrations be idempotent or include a 'backup before migrate' step in the local dev script. Document how to roll back to previous migration (drizzle-kit limitations) and provide an automated 'export DB' script run before applying migrations.
330: - **Line 46**: [moderate] Scalability and UX: no pagination, rate-limiting, or limits described. A local DB can grow large and fetching full history on the Server Component may become slow or memory-heavy.
331:   - *Suggestion*: Add pagination (cursor-based) and a page size default (e.g., 50). Ensure indices support filter+sort queries. Consider filtering presets (last 30 days) and an export option for very large histories.
332: - **Line 11**: [minor] No mention of error telemetry/local logs for debugging. For local-first apps it's helpful to have local logs or a way to surface errors beyond toasts.
333:   - *Suggestion*: Add a simple client-side and server-side logging pattern (console + optional file-based logs) and surface last fatal error in UI (admin diagnostics page). This can be basic and local-only (no external services).
334: - **Line 48**: [minor] Testing scope beyond unit tests is underspecified. The plan requires unit tests for chore-engine but lacks integration/E2E tests for Server Actions, DB migrations, and optimistic UI flows.
335:   - *Suggestion*: Add at least: (1) integration tests that run against an ephemeral SQLite DB (CI/local script) to exercise Server Actions and migrations, and (2) an optional E2E test (Playwright) for critical flows: add user, add chore, mark-done (optimistic path + failure reconciliation), and history filtering. If not CI, provide a 'test-local' npm script that runs them.
336: - **Line 31**: [moderate] Edge-case: empty household or no eligible users. The acceptance tests mention 'empty households' but the UI behavior is not specified.
337:   - *Suggestion*: Define explicit UX for empty households: dashboard should show CTA to add members and mark 'no eligible assignee' states clearly. Chore-engine should return an explicit 'no_assignee' value so UI can present the proper CTA.
338: - **Line 16**: [moderate] Avatar handling & security is unspecified. If you accept external URLs or uploads, you need sanitization, size limits, and storage clarity (local vs remote).
339:   - *Suggestion*: If avatars are only URL strings, validate origins and use Content Security Policy guidance. If supporting uploads, plan local storage path, filename sanitization, resizing on upload, and cleanup on user deletion. Document whether images are stored on the DB as blobs (not recommended) or on disk.
340: 
341: ## Comments
342: 