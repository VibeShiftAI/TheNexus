{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Loader window (macOS) and travel-shell webviews (Windows). IPC reaches only local app pages (tab strip + per-tab loaders); once a loader hands over to a remote tunnel origin, that content has no Tauri access.",
  "windows": ["main"],
  "webviews": ["*"],
  "permissions": ["core:default"]
}
