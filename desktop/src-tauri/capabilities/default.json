{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Loader window (macOS) and travel-shell tab strip (Windows). Only local app pages get IPC; the remote content webviews (bridge/studio through the tunnel) have no Tauri access.",
  "windows": ["main"],
  "webviews": ["chrome"],
  "permissions": ["core:default"]
}
