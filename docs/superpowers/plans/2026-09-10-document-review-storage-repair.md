# Document review receiver storage repair

Goal: satisfy the failed durable-delivery criterion without reopening settled UI/security criteria. The main reviewer remains in TheNexus; the additional Praxis repository owns the receiving ledger. This bounded repair is authorized for direct implementation with no design approval, commit, push or task-status mutation.

1. Reproduce the supplied isolated storage-failure command before editing. Record the original dirty state and copies of each contended file in `/tmp/nexus-ledger-repair-baseline`.
2. Extend `Praxis/tests/chat_idempotency.test.ts` first: claim rename failure must return 503 with zero agent calls and recover safely after cache loss; receipt rename failure must return 409 uncertainty with one call, and stay blocked after cache loss/new boot. Change unsafe abandoned/failed/stale replay expectations to no replay. Run `npm test -- chat_idempotency` and observe those assertions fail.
3. Change `Praxis/src/chat-idempotency.ts`: persist candidate maps before publishing claims/completions to cache; sync file and directory; propagate storage faults. Retain uncertain claims on failed completion, distinguish active handlers from disk claims, refuse replay of abandoned/failed records, and fail closed on unreadable ledgers. Remove automatic eviction that could erase evidence of uncertain processing.
4. Change only the idempotency wrapper in `Praxis/src/routes/chat.ts`: initial storage refusal is 503 before processing; failed completion or interrupted processing is explicit nonretryable 409 uncertainty. Preserve unkeyed routes. Exercise storage and restart boundaries through the real isolated route.
5. Preserve the reviewer's browser check under Nexus `scripts/document-review-qa/`, including its isolated fixtures; compare parsed URL pathnames so cache-busting query strings cannot hide polls. Run it in a throwaway stack if available. Preserve the settled reviewer implementation.
6. Run focused Nexus document suites, focused dashboard reviewer/Markdown/task-panel suites, Praxis chat tests and typecheck. Inspect this run's exact diffs against saved pre-edit bytes; run strict UTF-8/mojibake checks. Record evidence, unchanged readiness blocker, operator URL, and every changed path/repository in a walkthrough. Leave cross-executor QA and lifecycle to the completion handler.
