# LangGraph Engine Requirements
# Python backend for TheNexus workflow orchestration

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
pydantic>=2.10.0
pydantic-settings>=2.0.0

# LangGraph & LangChain Core
langgraph>=0.2.60
langchain>=0.3.0
langchain-core>=0.3.0

# AI Providers
langchain-google-genai>=2.0.0
langchain-anthropic>=0.3.0
langchain-openai>=0.2.0

# LangGraph Persistence (SQLite)
langgraph-checkpoint>=2.0.0
langgraph-checkpoint-sqlite>=2.0.0
aiosqlite>=0.20.0

# HTTP Client (for calling Node.js backend)
httpx>=0.28.0

# Utilities
python-multipart>=0.0.18

# Research Tools
beautifulsoup4>=4.12.0
ddgs>=9.0.0

# Graph Memory (Neo4j / Graphiti)
neo4j>=5.0.0
graphiti-core>=0.1.0

# Cortex Brain (System 2)
fastmcp>=3.0.0
ruamel.yaml>=0.18.0

# Media adapters (YouTube channel workflow)
# YouTube upload adapter uses googleapiclient; OAuth via google-auth-oauthlib.
# Install before running `python -m tools.media.youtube --authorize`.
google-api-python-client>=2.0.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
